# Per MCP spec, STDIO servers retrieve credentials from the environment.
# Bookface uses magic-link auth (no password), so the credential is a session cookie.
#
# Option A (recommended): Store cookie in macOS Keychain via setup script
#   npx tsx src/setup.ts
#
# Option B: Set the cookie directly as an env var
#   YC_SESSION_COOKIE=_bf_session_key=...; XSRF-TOKEN=...

# Required for the get_new_messages tool
YC_USER_ID=your_numeric_user_id_here
